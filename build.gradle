allprojects {
  apply plugin: 'java'

  version "1.1"
  sourceCompatibility = 1.8
}

subprojects {
  repositories { mavenCentral() }
  dependencies {
    testCompile 'junit:junit:4.11'
  }
}

jar.enabled = false